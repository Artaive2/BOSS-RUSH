//Draw the health of the boss to the window
//draw_text_transformed( (window_get_width() / 2) - 40, window_get_height() - 100, string(the_health), 4, 4, 0);


//TO DELETE
//draw_text(50, 500, string(attack_timer) );
//draw_text(50, 300, string(distance_to_object(o_player)) );